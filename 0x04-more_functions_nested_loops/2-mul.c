#include "main.h"
#include <stdio.h>

/**
 * mul - mltiplies two integers
 * @a: integer to multiply
 * @b: integer to multiply
 * Return: Always 0
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
